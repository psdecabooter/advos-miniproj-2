# built binaries
timer-test
latency-test
throughput-test

# benchmark results, root only -- results/ is committed
/*.csv

# latex build products
report/*.aux
report/*.fdb_latexmk
report/*.fls
report/*.log
report/*.synctex.gz
report/*.out
report/*.toc

# macOS
.DS_Store
